const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ include: { activeCompany: true } }); console.log(users.map(u => ({ email: u.email, activeCompanyId: u.activeCompanyId }))); } main().then(() => process.exit(0));
