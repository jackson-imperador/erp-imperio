const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const start = new Date(Date.UTC(2026, 8, 1)); const end = new Date(Date.UTC(2026, 9, 1)); const validOrders = await prisma.saleOrder.findMany({ where: { companyId: '1a96b12c-389b-4593-85d9-4a0ed5e6d44d', status: { in: ['CONFIRMED', 'COMPLETED'] }, confirmedAt: { gte: start, lt: end } }, include: { payments: true } }); console.log('SALES COUNT:', validOrders.length); } main().then(() => process.exit(0));
